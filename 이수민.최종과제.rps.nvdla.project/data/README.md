# Dataset

The RPS dataset is maintained in:

```text
https://github.com/s0o0omin/RP2-RPS-QAT-Lab
```

Run `download_dataset.sh` to place it at `source/RPS_Dataset`. The dataset is
not duplicated in this submission repository.
